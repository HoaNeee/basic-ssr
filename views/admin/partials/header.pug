header
  .container-fluid
    .inner-header-content
      .inner-logo
        a(href=`${prefixAdmin}/` style="font-weight: 700") ADMIN 
      .inner-nav 
        a(href=`${prefixAdmin}/auth/logout` class="btn btn-danger") Đăng xuất   