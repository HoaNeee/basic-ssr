mixin sub-menu(records, path)
  ul
    each item in records 
      li 
        a(href=`${path}/${item.slug}`) #{item.title}
        - if(item.childArray && item.childArray.length > 0)
            +sub-menu(item.childArray, path)